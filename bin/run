#!/usr/bin/env php
<?php

use Monolog\Handler\StreamHandler;
use Monolog\Logger;
use Phplrt\LanguageServer\LanguageServer;

require __DIR__ . '/../vendor/autoload.php';

$logger = new Logger('ZendDebug', [
    new StreamHandler(STDOUT),
]);

$lsp = new LanguageServer($logger);
$lsp->tcp();
